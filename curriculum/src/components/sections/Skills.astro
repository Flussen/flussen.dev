---
// src/components/SkillsBadges.astro

import GolangIcon from "../icons/golang.astro";
import JavaIcon from "../icons/java.astro";
import PostgresIcon from "../icons/postgresql.astro";
import GitIcon from "../icons/git.astro";
import GitHubIcon from "../icons/GitHub.astro";
import LinuxIcon from "../icons/linux.astro";
import DockerIcon from "../icons/docker.astro";
import Section from "../Section.astro";
import { getTranslation, type Locale } from "@/lib/i18n";

interface Props {
  cvData: any;
  locale: Locale;
}

const { cvData, locale } = Astro.props;

type Skill = {
  name: string;
  Icon: any;
};

const skills: Skill[] = [
  { name: "Golang", Icon: GolangIcon },
  { name: "Java", Icon: JavaIcon },
  { name: "PostgreSQL", Icon: PostgresIcon },
  { name: "Git", Icon: GitIcon },
  { name: "GitHub", Icon: GitHubIcon },
  { name: "Linux", Icon: LinuxIcon },
  { name: "Docker", Icon: DockerIcon },
];
---

<Section title={getTranslation(locale, "Skills")}>
  <div class="flex flex-wrap gap-1.5 sm:gap-2">
    {skills.map(({ name, Icon }) => (
      <div
        class="inline-flex items-center rounded-md border border-border/60
                bg-card/60 backdrop-blur px-2 py-0.5 sm:px-3 sm:py-1 text-xs sm:text-sm font-medium text-foreground/90
               shadow-sm">
        <span
          class="flex h-4 w-4 sm:h-6 sm:w-6 items-center justify-center mr-1 sm:mr-0.5">
          <Icon />
        </span>
        <span>{name}</span>
      </div>
    ))}
  </div>
</Section>

